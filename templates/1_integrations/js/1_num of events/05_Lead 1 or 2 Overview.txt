OVERVIEW

${accountName} to implement impact.com's Universal Tracking Tag, Identify function, and trackConversion function for tracking ${andEventNames} events on ${siteDefinition}.  
The scripts can be deployed via a tag manager or placed directly on the site.

EVENT NAME = ${multiEventTypesNameAndId}
METHOD = Javascript
SITE DEFINITION = ${siteDefinition}
